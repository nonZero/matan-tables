To run in hot module reloading mode:

```sh
npm start
```

To create a production build:

```sh
npm run build-prod
```

Run json server:

    yarn

or

    npm install
    
and then:

    json-server db.json
